import { MissingInformationPipe } from './missing-information.pipe';

describe('MissingInformationPipe', () => {
  it('create an instance', () => {
    const pipe = new MissingInformationPipe();
    expect(pipe).toBeTruthy();
  });
});
